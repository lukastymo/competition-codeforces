package codeforces

import scala.io.StdIn._

object C extends App {
  val t = readInt()
  1 to t foreach { _ =>
    val S = readLine().toCharArray
    val T = readLine().toCharArray

    val xs = S.zipWithIndex

  }
}
